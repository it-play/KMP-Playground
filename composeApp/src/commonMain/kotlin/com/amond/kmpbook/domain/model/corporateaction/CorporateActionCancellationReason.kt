package com.amond.kmpbook.domain.model.corporateaction

/** Typed reason that permanently closes an announced corporate-action occurrence. */
enum class CorporateActionCancellationReason {
    LISTING_LIFECYCLE,
    PRODUCT_STATE_INELIGIBLE,
}
