package com.amond.kmpbook.domain.model.fund

/** Candidate ranking rule applied only on a scheduled reconstitution. */
enum class FundOfFundsSelectionModel {
    DISTRIBUTION_DISCOUNT_LIQUIDITY,
    ACTIVE_INCOME_ROTATION,
}
